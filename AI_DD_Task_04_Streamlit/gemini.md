You are an AI code generator.
Generate clean and modular Python code.

Files required:
1. pdf_summarizer.py → A function that takes PDF text and generates a summary using Gemini API.
2. quiz_generator.py → A function that generates MCQs or quiz items.
3. utils.py → Optional helper functions.
4. app.py → Streamlit UI to run PDF summarizer and quiz generator.

Rules:
- Use pythonic code
- Functions must return outputs cleanly
- No hard-coded file paths
- Use google-generativeai for API calls
- Keep code simple for beginners